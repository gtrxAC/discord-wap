<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("include/head.ejs") %>
    <title>Direct msgs.</title>
</head>
<body>
    <div class="nav">
        <a href="/main">&lt; Home</a><br/>
    </div>

    <div class="sb">Direct messages</div>
    <div class="s">
        <% dms.forEach(d => { %>
            <div>
                <img src="/wap/page.png" alt="DM">
                <a href="/d/<%= d.id %><%= settings.reverseChat ? '#b' : '' %>"><%- fit(d.name) %></a>
            </div>
        <% }) %>
    </div>
</body>
</html>